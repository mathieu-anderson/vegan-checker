{
  "name": "vegan-checker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "fuzzy": "^0.1.3",
    "is-not-vegan": "^1.2.3",
    "npm": "^6.1.0",
    "react": "^16.4.1",
    "react-autocomplete": "^1.8.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && surge --project ./build --domain vegan-checker.surge.sh"
  },
  "devDependencies": {
    "prop-types": "^15.6.2"
  }
}
